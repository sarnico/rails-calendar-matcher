<!DOCTYPE html>

<html>

  <head>

    <meta charset="UTF-8">

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>Calendar Matcher</title>

    <%= csrf_meta_tags %>

    <%= action_cable_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

    <%= javascript_include_tag 'application' %>

    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->

    <%= favicon_link_tag asset_path('CalendarMatcher.svg') %>

  </head>

  <body id="body">

      <%= render "shared/navbar" %>

      <%= yield %>

      <%# This refresh all the events of the user so he doesnt need to go in his calendar to refresh them %>

      <% if current_user != nil %>

       <% GoogleRefresh.refresh_all(current_user) %>
       <% @userevents = current_user.reload.user_events %>

      <% end %>

      <%= javascript_pack_tag 'application' %>

      <%= stylesheet_pack_tag 'application'  %>

  </body>

</html>
