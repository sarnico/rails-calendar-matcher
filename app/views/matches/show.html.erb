<%# Uncomment all the lines below to actualize everybody's calendar before a match %>
<%# It doesn't work yet because we need to actualize other people calendar on match %>

<%# @users = User.all %>

<%# @users_id_for_match = @match.user_ids %>

<%# @users_match = @users_id_for_match.each do |user| %>

  <%# @users.find(user) %>

<%# end %>

<%# @bigtest = @users_match.each do |user| %>

  <%# GoogleRefresh.refresh_all(user) %>

  <%# user.reload.user_events %>

<%# end %>

<div class="green-bottom-background">

    <div class="white-top-background"></div>

    <div class="container box-new content-main">

      <h3 class="text-center">Select a day</h3>

      <div class="container cal">

        <div
          id="calendar_matches"
          data-ids="<%= current_user.email %>"
          data-results="<%= @results.to_json %>"
          data-matchid="<%= @match.id.to_json%>"
          class="container"
        >

      </div>

    </div>

    </div>

</div>

